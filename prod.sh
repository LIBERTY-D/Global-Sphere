#!/usr/bin/env bash

#check for  env variables if set
#ENV_FILE=.env.staging docker-compose --env-file .env.staging config
##APP_PORT=9090 ENV_FILE=.env.staging docker-compose --env-file .env.staging up
# --build
#ENV_FILE=.env.staging docker-compose --env-file .env.staging up --build